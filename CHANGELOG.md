# Changelog

## 0.1.0

- Added Anchor workspace scaffold for the Solana Distribution Protocol.
- Added empty `create_stream`, `withdraw`, and `cancel` instruction handlers.
- Added Week 2 architecture state model through `VestingState` and `DistributionKind`.
- Added a TypeScript integration test that calls `create_stream`.
- Added GitHub Actions CI for build and test.
- Added README setup, build, devnet deploy, and test instructions.
